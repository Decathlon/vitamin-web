import { Meta, Story, Canvas } from '@storybook/addon-docs';
import vitamixIcons from '@vtmn/icons/dist/vitamix/font/vitamix.json';

# Overview

<Meta title="Icons / Overview" />

<Canvas>
  {Object.keys(vitamixIcons).map((icon) => (
    <span
      key={icon}
      aria-label={icon}
      className={`vtmx-${icon}`}
      style={{ fontSize: 32 }}></span>
  ))}
</Canvas>
