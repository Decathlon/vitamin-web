import { Meta, Story, Canvas } from '@storybook/addon-docs';
import '@vtmn/assets/dist/assets/sprite/assets.svg';
import assets from '@vtmn/assets/dist/assets/json/assets.json';

# Overview

<Meta title="Assets / Overview" />

The list of assets.
Tip: click on an asset and it will be copied to your clipboard! 📎

<Canvas>
  <div
    style={{
      display: 'flex',
      flexWrap: 'wrap',
      justifyContent: 'center',
    }}
  >
    {Object.keys(assets).map((asset) => (
      <svg width="16" height="16" aria-hidden="true" focusable="false">
        <use href={`#${asset}`} />
      </svg>
    ))}
  </div>
</Canvas>
