import '@testing-library/jest-dom';

import { render, fireEvent } from '@testing-library/svelte';
import VtmnPopover from '../VtmnPopover.svelte';

describe('VtmnPopover', () => {
  test('', () => {});
});
