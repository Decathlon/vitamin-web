import '@testing-library/jest-dom';

import { render, fireEvent } from '@testing-library/svelte';
import VtmnCheckbox from '../VtmnCheckbox.svelte';

describe('VtmnCheckbox', () => {
  test('', () => {});
});
